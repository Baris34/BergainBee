/**
 * Below are the colors that are used in the app. The colors are defined in the light and dark mode.
 * There are many other ways to style your app. For example, [Nativewind](https://www.nativewind.dev/), [Tamagui](https://tamagui.dev/), [unistyles](https://reactnativeunistyles.vercel.app), etc.
 */

const tintColorLight = '#0a7ea4';
const tintColorDark = '#fff';

export default {
  primary: '#FFC107', // Örnek birincil renk (sarı)
  secondary: '#E91E63', // Örnek ikincil renk (pembe)
  text: '#212121', // Örnek metin rengi (koyu gri)
  background: '#FFFFFF', // Örnek arka plan rengi (beyaz)
  grey: '#BDBDBD', // Örnek gri renk
  lightGrey: '#EEEEEE', // Örnek açık gri renk
  white: '#FFFFFF',
  black: '#000000',
  tabIconDefault: '#888', // Örnek sekme ikonu rengi (pasif)
  tabIconSelected: '#FFC107', // Örnek sekme ikonu rengi (aktif)
};
